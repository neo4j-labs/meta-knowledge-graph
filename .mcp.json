{
  "mcpServers": {
    "metagraph-mcp": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PROJECT_DIR}",
        "--no-sync",
        "metagraph-mcp"
      ]
    }
  }
}
